Visual Studio 2013 basic ASP.NET template
This will take bower main files, copy to wwwroot/lib for jQuery
it will then use wiredep to wire up dependencies (scripts) and inject script tags into index.html
It will smartly determine the correct order to include scripts.